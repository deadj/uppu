<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta http-equiv="Content-Type" content="text/html; charset=utf-8">
    <meta name="viewport" content="width=device-width, initial-scale=1">
    <title>Uppu</title>
    <link rel="stylesheet" href="/css/main.css">
    <link rel="stylesheet" href="/css/file.css">
    <script src="//ajax.googleapis.com/ajax/libs/jquery/2.1.4/jquery.min.js"></script> 
</head>
<body>
    {% include 'DuplicateItems/header.phtml' %}
    {% if notify is defined %}
        <div class="emptyCommentNotify">
            В комментарии должно быть что-то написано
        </div>
    {% endif%}
    <input type="hidden" id="uploadStatus" value="{{ filesData.uploadIsDone }}">
    <input type="hidden" id="fileId" value="{{ filesData.nameId }}">
    <div>
        <div class="fileBlock">
            {% if filesData.uploadIsDone == "done" %}
                <div class="content">
                    {% if filesData.type == "video" %}
                        <video src="/{{ filesData.link }}" controls></video>
                    {% elseif filesData.type == "audio" %}
                        <audio src="/{{ filesData.link }}" controls></audio>
                    {% elseif filesData.type == "image" %}
                        <img id="imgFile" src="/{{ filesData.link }}" width="350">
                    {% endif %}
                </div>
                <div class="fileInfo">
                    <ul class="dataNames">
                        <li>Название</li>
                        <li>Размер</li>
                        <li>Дата загрузки</li>
                        <li>Комментарий</li>
                        {% for data in filesData.metadata %}
                            <li>{{ data.dataName }}</li>
                        {% endfor %}
                    </ul>
                    <ul class="dataValues">
                        <li>{{ filesData.name }}</li>
                        {% if size <= 1000 %}
                            <li>{{ filesData.size }} Кб.</li>
                        {% else %}
                            <li>{{ filesData.size / 1000}} Мб.</li>
                        {% endif %}
                        <li>{{ filesData.date }}</li>
                        {% if filesData.comment is empty%}
                            <li>-</li>
                        {% else %}
                            <h1>filesData.comment</h1>
                        {% endif %}
                        {% for data in filesData.metadata %}
                            <li>{{ data.value }}</li>
                        {% endfor %}
                    </ul>
                </div>
                <a class="uploadButton" href="/{{ filesData.link }}" download="">Скачать</a>
            {% elseif filesData.uploadIsDone == "error" %}
                <p>Извините, файл загружен с ошибкой. Страница скоро будет удалена. Попробуйте загрузить снова.</p>
            {% else %}
                <img src="/img/ajax-loader.gif"> Файл обрабатывается
            {% endif %}
        </div>
    </div>
    {% if comments|length > 0 %}
        <div class="commentsBlock" id="commentsBlock">
            {{ _self.printComments(comments, filesData.nameId) }}
        </div>
    {% else %}
        <div class="commentsBlock" id="commentsBlock">
            <p>Ваш комментарий будет первым</p>
        </div>
    {% endif %}

    <form method="post" action="/addComment" class="newCommentBlock" id="newCommentBlock">
        <input type="hidden" name="fileId" value="{{ filesData.nameId }}">
        <input type="hidden" name="parentId" value="NULL">
        <input type="hidden" name="notjs">
        <textarea name="comment" id="newCommentsText" required></textarea>
        <button id="addCommentButton">Отправить</button>
    </form>
    <script type="text/x-template" id="templateReplyComment">
        <textarea class="replyCommentField" name="comment"></textarea>
        <button id="replyCommentButton" onclick="replyComment(this)">Отправить</button>
    </script>
    <script type="text/x-template" id="templateComment">
        <input type="hidden" name="commentId" value="[[id]]">
        <h5>[[date]]</h5>
        <p>[[text]]</p>
    </script>
    <script src="/js/forFiles/file.js"></script>
</body>
</html>

{% macro printComments(comments, fileId) %}
    {% for comment in comments %}
        <div class="comment">
            <input type="hidden" name="commentId" value="{{ comment.id }}">
            <h5>{{ comment.date }}</h6>
            <p>{{ comment.text|nl2br }}</p>
            <label class="replyBlock">
                <input type="radio" name="radio" class="replyRadio">
                <p class="openReplyButton" onclick="openReply(this)">Ответить</p>
                <p class="closeReplyButton" onclick="closeReply(this)">Закрыть</p>
                <form method="post" action="/addComment" class="replyComment">
                    <input type="hidden" name="fileId" value="{{ fileId }}">
                    <input type="hidden" name="parentId" value="{{ comment.id }}">
                    <input type="hidden" name="notjs">
                    <textarea class="replyCommentField" name="comment" required></textarea>
                    <button>Отправить</button>
                </form>
            </label>
            {% if comment.children|length > 0 %}
                {{ _self.printComments(comment.children, fileId) }}
            {% endif %}
        </div>
    {% endfor %}
{% endmacro %}